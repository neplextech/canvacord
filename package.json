{
  "name": "canvacord-monorepo",
  "private": true,
  "scripts": {
    "format": "prettier --write packages/**/*.{js,ts,jsx,tsx}",
    "prepare": "husky install",
    "build":"lerna exec npm run build",
    "symlink":"lerna bootstrap"
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "lerna": "^4.0.0",
    "prettier": "^2.4.1"
  }
}
